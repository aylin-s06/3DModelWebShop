# Дебъгване на проблеми с автентикация и админ роля

## Проблем: Не се появяват администраторските страници и logout бутон

### Стъпка 1: Проверете в Console (F12)

Отворете Developer Tools (F12) → Console и проверете за следните съобщения:

1. **При зареждане на страницата:**
   ```
   ✅ User loaded: {username: '...', role: 'ADMIN', ...}
   ```

2. **В Navbar:**
   ```
   🔍 DEBUG Navbar State: {
     user: {...},
     userRole: 'ADMIN',
     isAdmin: true,
     isAuthenticated: true,
     hasToken: true
   }
   ```

### Стъпка 2: Проверете ролята в базата данни

```sql
-- Проверете вашия потребител
SELECT id, username, email, role FROM users;

-- Ролята трябва да е точно 'ADMIN' (с главни букви)
-- Ако не е, променете я:
UPDATE users SET role = 'ADMIN' WHERE id = [вашето_ID];
```

### Стъпка 3: Проверете JWT токена

В Console (F12) напишете:

```javascript
// Проверете токена
const token = localStorage.getItem('token');
console.log('Token:', token);

// Декодирайте токена
if (token) {
    const payload = JSON.parse(atob(token.split('.')[1]));
    console.log('Token payload:', payload);
    console.log('Role in token:', payload.role);
    console.log('User ID:', payload.userId);
    console.log('Username:', payload.sub || payload.username);
}
```

### Стъпка 4: Проверете дали потребителят е зареден

В Console (F12) напишете:

```javascript
// Проверете React DevTools или временно добавете в Navbar.js:
// console.log('Auth State:', { user, isAuthenticated, isAdmin: isAdmin() });
```

### Стъпка 5: Излезте и влезте отново

**Много важно:** След промяна на ролята в базата данни:

1. **Излезте от системата:**
   - В Console напишете: `localStorage.removeItem('token')`
   - Или натиснете logout бутон (ако е видим)

2. **Презаредете страницата:**
   - Натиснете F5 или Ctrl+R

3. **Влезте отново:**
   - Отидете на `/login`
   - Влезте с вашите данни

### Стъпка 6: Проверете Network tab

1. Отворете Developer Tools (F12) → Network
2. Презаредете страницата
3. Намерете заявката към `/api/users/{id}` или `/api/users`
4. Проверете Response - трябва да видите `"role": "ADMIN"`

## Често срещани проблеми:

### Проблем 1: `isAuthenticated` е `false`
**Причина:** `user` не е зареден или `token` липсва
**Решение:** 
- Проверете дали има token в localStorage
- Проверете дали `/api/users/{id}` endpoint работи
- Проверете Console за грешки

### Проблем 2: `user.role` не е `'ADMIN'`
**Причина:** Ролята в базата данни не е правилно зададена
**Решение:**
```sql
UPDATE users SET role = 'ADMIN' WHERE id = [вашето_ID];
```
След това излезте и влезте отново.

### Проблем 3: Logout бутонът не се показва
**Причина:** `isAuthenticated` е `false` или `user` е `null`
**Решение:**
- Проверете дали потребителят е зареден правилно
- Проверете Console за debug съобщенията
- Проверете дали има token в localStorage

### Проблем 4: Админ страниците не се показват
**Причина:** `isAdmin()` връща `false`
**Решение:**
- Проверете дали `user.role === 'ADMIN'`
- Проверете дали сте излезли и влезли отново след промяна на ролята
- Проверете Console за debug съобщенията

## Бърз тест:

1. Проверете в MySQL: `SELECT id, username, role FROM users;`
2. Променете ролята: `UPDATE users SET role = 'ADMIN' WHERE id = X;`
3. В Console: `localStorage.removeItem('token')`
4. Презаредете страницата: F5
5. Влезте отново на `/login`
6. Проверете Console за debug съобщенията
7. Проверете дали виждате "Админ панел" в Navbar

